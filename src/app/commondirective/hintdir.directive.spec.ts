import { HintdirDirective } from './hintdir.directive';

describe('HintdirDirective', () => {
  it('should create an instance', () => {
    const directive = new HintdirDirective();
    expect(directive).toBeTruthy();
  });
});
