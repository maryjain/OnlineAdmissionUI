import { CustomvalidatordirectiveDirective } from './customvalidatordirective.directive';

describe('CustomvalidatordirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomvalidatordirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
