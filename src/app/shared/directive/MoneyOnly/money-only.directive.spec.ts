import { MoneyOnlyDirective } from './money-only.directive';

describe('MoneyOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new MoneyOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
