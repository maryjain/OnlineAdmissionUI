import { CGPADirective } from './cgpa.directive';

describe('CGPADirective', () => {
  it('should create an instance', () => {
    const directive = new CGPADirective();
    expect(directive).toBeTruthy();
  });
});
